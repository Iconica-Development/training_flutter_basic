# Flutter basic training slides

These slides are used by Iconica trainers to give the Flutter basic training.

To create these slides `lite-server` (Node package) is used.
You can run them using these commands:

```sh
npm install
npm run
```